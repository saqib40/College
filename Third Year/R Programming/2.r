# R program to find the maximum and the minimum value of a given vector.

x <- c(10, 20, 30, 25, 9, 26)
cat("Original Vectors:",x)
cat("Maximum value of the above Vector:",max(x))
cat("Minimum value of the above Vector:",min(x))
